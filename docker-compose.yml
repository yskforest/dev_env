services:
  cpu:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        UID: "${UID:-1000}"
        GID: "${GID:-1000}"
    volumes:
      - ${HOME}:/workspace
    working_dir: /workspace
    restart: always
    environment:
      - DISPLAY=${DISPLAY}
    network_mode: "host"
    tty: true
    stdin_open: true

  gpu:
    build:
      context: .
      dockerfile: Dockerfile.gpu
      args:
        UID: "${UID:-1000}"
        GID: "${GID:-1000}"
    volumes:
      - ${HOME}:/workspace
    working_dir: /workspace
    environment:
      - DISPLAY=${DISPLAY}
    network_mode: "host"
    tty: true
    stdin_open: true
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [ gpu ]
